# RIT-083: Numerology AI interpretation and evaluations

- Backlog item: RIT-083
- Related records: D-037, D-038, D-040, D-041, D-043, D-064, D-065
- Owner approval: OWN-012

The queue state, priority, milestone, dependencies, and owner gates remain canonical in
[BACKLOG.md](../../BACKLOG.md).

## Outcome

The repository has a safe-off English numerology interpretation boundary that recomputes RIT-081
facts, exposes only a privacy-minimized fact projection, requires independently verified and
authorized content/prompt/fallback artifacts, binds a single-use candidate by digest, rejects
number drift and unsafe prose, requires an independent semantic reviewer before display, and
provides a checksum-bound zero-tolerance synthetic evaluation gate.

D-065 resolves OWN-012 with a checksummed, rights-cleared, owner-reviewed English corpus covering
every calculation/result pair plus exact prompt, fallback, reviewer, and paid-product records. The
approved catalog projects through the AI boundary without weakening its fail-closed rules.
RIT-083 is `Done` for repository implementation while provider, runtime, Credits, and production
activation remain safe-off.

## Scope

Included: exact engine-fact recomputation; full thirty-six-pair content inventory for Life Path,
Birthday Number, and Personal Year across all reachable values; birth-date, digit, initial-sum,
and reduction-step minimization; strict English prompt/output/fallback schemas; separate SHA-256
integrity and server-owned authority checks; exact number, target-year, source, locale, schema,
and artifact binding; digit and number-word exclusion from model prose; single-use pending
candidates; digest and input-identity binding; deterministic whole-output safety checks;
independent strict semantic review; deterministic approved safe replacement; privacy-safe
metadata; synthetic ordinary and master-number controls; approved content projection; and a
dedicated fixed release evaluator integrated into the existing AI CI command.

Excluded: a provider or reviewer model, provider SDK/key/network request, paid inference, Credits
reservation/consumption, database persistence, API, calculator AI UI, another locale, deployment,
and public launch.

## Acceptance status

- [x] AI input is derived only after exact RIT-081 fact recomputation and excludes birth date,
      canonical digits, initial sums, reduction steps, private questions, identity, journal, and
      payment data.
- [x] Candidate output cannot alter, omit, add, reorder, narrate, or translate any result or target
      year; all numeric authority remains in exact server-verified structured fields.
- [x] Content, prompt, and fallback artifacts require separate exact integrity and authorization
      checks, and content covers every reachable calculation/result pair before selection.
- [x] Output schema, source, safety, locale, markup, Unicode/confusable, reviewer, digest,
      single-use, trust-failure, safe-control, privacy, and deterministic-fallback tests pass.
- [x] The fixed synthetic evaluation gate requires zero critical, metric, safe-control, missing, or
      unexpected failures and zero external or paid calls.
- [x] Exact English meanings, source provenance, AI-use rights, review, prompt/fallback, and
      paid-product mapping are owner-approved through OWN-012 and D-065.
- [x] The approved catalog projects through the AI boundary without weakening D-064 or exposing
      the free calculator to a paid Deep Reading feature.
- [x] Product `year_reflection`, fulfillment `deep_reading.year_reflection`, and six-Credit cost
      are bound as optional verified context with activation fixed to `safe_off`.

## Verification evidence

The focused numerology AI contract passes 34 assertions covering ordinary output, all
`11`/`22`/`33` fallback controls, fact tamper, complete content inventory, SHA-256 integrity,
authority denial, content, prompt, fallback, source, locale, schema, digit/number-word prose,
every prohibited safety category, hostile Unicode/markup, safe negation, independent reviewer,
candidate digest, single-use, trust failure, and privacy metadata.

`pnpm test:numerology-ai-evals` binds the exact suite bytes to a metadata-only safe-off baseline
and passes all 32 registered cases through 39 executed assertions in two files. The combined
`pnpm test:ai-evals` retains the Tarot gate and then runs the numerology gate. Both use zero
external requests and zero paid calls.

The D-065 publication tests additionally pass the exact approved thirty-six-entry corpus through
the RIT-083 artifact parser, SHA-256 verifier, authority verifier, deterministic fact
recomputation, and calculation-specific entry selection. The selected facts remain exactly Life
Path `3`, Birthday Number `1`, and Personal Year `4` for the synthetic control.

Changed-file formatting, focused tests, Web type checking, the Web production build, architecture,
CI-contract, and secret checks provide task closure evidence. The complete root build still has
the pre-existing static `/en` artifact assumption introduced before RIT-082 made that route
dynamic, so this task does not claim a false root-build pass. Generated-evidence synchronization
cannot run without representing unrelated canonical untracked files in the Git index; this task
does not stage them. No database or personalized browser gate applies because no runtime
interpretation route, persistence, provider, or paid UI is activated.

## Approval resolution

The owner approved option A on 2026-07-25. D-065 binds the exact catalog version and file digest,
all thirty-six meanings, worldwide owned-source uses, author/reviewer records, prompt, fallback,
semantic reviewer registration, English scope, and optional six-Credit
`year_reflection`/`deep_reading.year_reflection` mapping.

This is content and repository approval, not production activation. Representative live-model
review, provider privacy settings, cost/latency/circuit-breaker controls, Credits lifecycle,
persistence, deployment, and public release still require their applicable implementation,
verification, and owner gates.

## Risks and rollback

The fixed synthetic evaluations prove code boundaries, not production-model quality, provider
behavior, or launch readiness. D-065 authorizes the exact English meanings but not a provider,
model, live reviewer, payment action, or public release.

Rollback disables or removes the safe-off approved-artifact projection and interpretation module
while retaining D-064 arithmetic and the deterministic calculator. There is no migration, stored
output, provider side effect, secret, payment, remote state, deployment, or personalized
customer-facing behavior to clean up.
