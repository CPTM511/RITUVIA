# RIT-004: Create the hosted CI quality gates

- Backlog item: RIT-004
- Related records: None
- Owner approval: the private GitHub remote and cumulative commit/push were owner-authorized on
  2026-07-29; production deployment and public source disclosure remain separately gated

The queue state, priority, milestone, dependencies, and owner gates remain canonical in
[BACKLOG.md](../../BACKLOG.md).

## Outcome

Establish the repository-owned GitHub Actions quality, PostgreSQL integration, and security jobs,
protect them on `main`, and capture one passing hosted run before closing the M0 CI foundation.

## Scope

Included: immutable GitHub Action references; read-only workflow permissions; locked dependency
installation; format, lint, type, unit, build, accessibility, browser, configuration, architecture,
records, migration, AI evaluation, editorial, localization, SEO/GEO, RTL, writing-system,
PostgreSQL, secret, dependency, native supply-chain, and Corresponding Source gates; protected
required checks; hosted evidence.

Excluded: production deployment, customer data, production secrets, DNS, indexing, payment or AI
provider activation, public repository visibility, and weakening any local or release gate.

## Acceptance criteria

- [x] The workflow defines separate `Quality`, `PostgreSQL integration`, and `Security scans` jobs.
- [x] Actions and the PostgreSQL service are pinned to immutable revisions or image digests.
- [x] The workflow uses read-only repository permission and no production secret or environment.
- [x] The PostgreSQL job builds its internal runtime dependency before executing the verifier.
- [x] The PostgreSQL job generates its Prisma client from a clean checkout before importing DB code.
- [x] Analytics verification builds its observability runtime dependency from a clean checkout.
- [x] The pinned actionlint downloader uses the official Linux AMD64 asset and checksum.
- [x] Full-history Gitleaks ignores only 11 exact reviewed test-constant fingerprints.
- [x] CI contract tests enforce the workflow and database-job boundaries.
- [x] A private GitHub remote exists and the historical `main` revision produced a hosted run.
- [ ] Required status checks and workflow changes are protected on `main`.
- [ ] One current hosted run passes all three required jobs.

## Verification evidence

- Hosted run `30466750711` exercised all three jobs against historical revision
  `2ff7a1c6106f4e78e34dfe283680701860ecd9cf` and exposed three real integration defects rather than
  being treated as passing evidence.
- PR `1` hosted run `30468813822` exercised the cumulative source tree and exposed three additional
  clean-runner assumptions: missing observability build output, missing generated Prisma client, and
  11 exact historical test constants classified by the default generic API-key rule.
- Hosted run `30469853925` proved the exact historical ignore list by scanning 40 commits with no
  leaks. It then exposed one additional i18n build prerequisite and an unrealistically short
  10-second native sanitizer compile bound; its PostgreSQL job was separately interrupted during
  dependency installation by repeated npm registry timeouts before any database command ran.
- Hosted run `30470928541` passed the complete `Security scans` job, including native sanitizer,
  SCA/CVE, and Corresponding Source checks. Quality reached formatting and identified two existing
  format drifts; PostgreSQL generated Prisma and built security before exposing the remaining
  missing domain build output.
- Hosted run `30471374114` passed the complete `Security scans` job again. Quality passed through
  lint and reached the root TypeScript gate, exposing the missing root JSX setting, one browser
  global shadowed by a local HTML string, and an incomplete declaration for the build-policy
  options. PostgreSQL completed all clean-runner prerequisites and migrations before the zero-drift
  assertion exposed Prisma's expected inability to represent the committed custom SQL constraints,
  explicit foreign-key names, indexes, and defaults.
- The current worktree fixes every hosted defect above. It also verifies Prisma's complete
  normalized clean-database SQL diff against a strict Prisma-7.8.0 fingerprint with SHA-256, byte,
  and line-count checks. The reviewed baseline is identical on a fresh throwaway database and the
  existing local migrated database; any additional, removed, reordered, or version-changed diff
  fails closed.
- Hosted run `30473143747` passed the complete `Security scans` job for a third consecutive current
  revision. PostgreSQL accepted the strict schema-drift baseline and reached migrated-database
  invariants, proving the clean migration and fingerprint path before one post-migration assertion
  failed under the generic stage label. Quality passed format, lint, and typecheck, then ran 1,963
  unit/contract tests: 1,955 passed, five skipped, and three configuration-contract assertions
  failed; 21 files also could not import clean-checkout Config/Security build outputs.
- The current worktree resolves the complete Quality failure set with source aliases for the exact
  Config and Security public exports, current native-script environment-reader inventory,
  configuration-backed publication identity, and a real post-launch astrology flag cleanup task.
  The database verifier now reports granular non-sensitive invariant stages without exposing
  credentials or database contents.
- Hosted run `30474266506` passed the complete `Security scans` job again. Quality passed format,
  lint, typecheck, architecture/evidence checks, 1,963 hosted unit/contract tests, fixed AI
  evaluations, and the configuration boundary before the production-artifact verifier counted a
  `nomodule` compatibility chunk against the modern JavaScript budget. PostgreSQL reached the
  granular `tarot runtime privilege attestation` stage and exposed that the CI-only writer role had
  broader `reading_report` insert privileges than the production runtime contract.
- The database foundation now grants whole-table insert only on `reading` and `tarot_draw`, and
  grants the writer exactly the 16 approved `reading_report` columns. A one-time loopback
  PostgreSQL role passes the same Tarot runtime privilege attestation locally, while the CI
  contract rejects any return to broad report insertion.
- The Web build policy continues to validate every referenced script asset, including
  `nomodule` compatibility code, but charges only modern-delivery scripts to the unchanged
  232-KiB JavaScript budget. The complete production build now passes with 121 workspace artifacts,
  45 public pages, five private experience pages, and 219,789 bytes maximum modern JavaScript gzip.
- Hosted run `30475576351` passed `Security scans` and proved the modern JavaScript budget
  correction by reaching the accessibility stage. PostgreSQL passed the corrected Tarot privilege
  attestation and then revealed that the CI policy inventory omitted the four current
  privacy-deletion read/update policies added by the authorization-hardening migration. Quality
  found a separate stale exact-equality assumption between the 45-page reviewed build inventory
  and the intentionally representative 14-route public accessibility smoke inventory.
- The database verifier now attests all current interpretation and verification policies,
  including their exact commands and privacy-deletion role. A read-only query against the
  one-time loopback migrated database matches the expected nine-policy inventory exactly.
- Accessibility now requires every representative smoke route to remain a subset of the complete
  reviewed build inventory. The GEO answer-context contrast exceptions remain limited to that
  component, use only contrast tokens already proven at or above 4.5:1 against every reviewed
  gradient, and fail if broad selectors escape the component or its bounded heading/source shape.
- Hosted run `30477604316` passed `Security scans` again. Quality passed evidence, format, and lint,
  then exposed the missing TypeScript declaration for the new accessibility-inventory audit before
  any unit or browser command ran. PostgreSQL passed the updated nine-policy inventory and reached
  feature-flag append-only behavior, where a historical fixed effective timestamp violated the
  existing `effective_at >= created_at` constraint before the intended policy assertion.
- The accessibility audit now has a matching typed declaration. Feature-flag CI fixtures obtain
  two bounded future timestamps from the PostgreSQL service clock, preventing runner-date drift and
  ensuring rejection assertions exercise row-level security rather than an unrelated timestamp
  constraint. A transaction on the one-time loopback database accepts the intended legacy
  safe-off row and rolls it back.
- `pnpm check:ci-contract` passes.
- `pnpm exec vitest run tests/ci-contract.test.ts tests/pinned-tool-contract.test.ts` passes 33
  focused tests.
- `pnpm --filter @rituvia/security build` passes.
- `pnpm check:ai-operations` builds observability and passes.
- `pnpm --filter @rituvia/db generate` produces the pinned Prisma 7.8 client.
- The pinned actionlint binary validates `.github/workflows/ci.yml` outside the restricted
  environment.
- Hosted Gitleaks scans 40 complete-history commits with no leaks after applying only the 11 exact
  reviewed fingerprints.
- Hosted run `30470928541` passes the complete `Security scans` job.
- The pinned cached-source native security run passes 151 mutation cases and 23-component supply
  chain validation after injecting only the trusted platform temporary directory into the
  sanitized compiler environment.
- Full repository `pnpm format:check` passes after correcting the two hosted findings.
- Domain, security, and pinned Prisma generation prerequisites pass locally in the hosted database
  command order.
- Root `pnpm typecheck` passes all 16 package typechecks plus the root TypeScript project.
- `pnpm exec vitest run packages/db/test/schema-drift-baseline.test.ts` passes four focused
  normalization, exact-match, mutation, shape, and version-drift checks.
- A fresh throwaway PostgreSQL database deployed all committed migrations and produced the reviewed
  normalized Prisma diff fingerprint
  `d90472e9ec55b07f404252805ad1ee8a6afa7c5f4cd9f54875ce6e6fd5d8a6ae` across 17,666 bytes and
  356 lines.
- Five representative clean-source unit files pass 28 tests across configuration contracts,
  Config/Security imports, Tarot publication, and ritual-reflection publication.
- Final local `pnpm test:unit` passes 2,144 tests across 183 files with five expected skips and no
  failures; this full matrix is run here because RIT-004 is the milestone CI integration task, not
  repeated for ordinary slices.
- The 103 focused CI contract, schema-drift, database-safety, and Web build-policy tests pass after
  the privilege and modern-budget corrections.
- `BRAND_CANONICAL_ORIGIN=http://127.0.0.1:4175 pnpm build` passes the complete production build
  and final artifact policy with the pinned Node 24.18.0 toolchain.
- `BRAND_CANONICAL_ORIGIN=http://127.0.0.1:4175 pnpm test:accessibility` passes all 14 public and
  three private route checks, 79 primary Axe scans, six deterministic Tarot scenarios, and the
  intention, ritual, Revisit, full-loop, and writing-system browser gates with no critical/serious
  violation, layout failure, unexpected request, private leak, or console/page error.
- The Web package typecheck and root TypeScript project pass with the accessibility declaration.
- The 68 focused accessibility, contrast, CI-contract, and database-safety tests pass.

## Risks and rollback

GitHub still returns HTTP 403 for private-repository branch protection until GitHub Pro is active
or the repository is made public. Public visibility is not implied by the project's AGPL license
and remains a separate owner decision. RIT-004 cannot be marked Done until protection and a
passing hosted run both exist.

Rollback reverts the CI closure commit or disables the candidate branch without changing
production systems. The protected historical `main` revision remains available, and no customer
data, production secret, deployment, DNS, or provider state is touched.
